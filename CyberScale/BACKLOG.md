# Backlog — CyberScale

Live task state. Updated at every working checkpoint, not at the end.

This is the working list: what is open, what was found mid-work, what was decided
and why. It is **not** the milestone plan — that is
[`docs/enhancement-roadmap.md`](docs/enhancement-roadmap.md), which tracks planned
version work. Items here are things discovered while doing something else, plus
decisions worth not relitigating.

**Status:** `OPEN` · `IN PROGRESS` · `BLOCKED` · `DONE` · `DROPPED` (with reason)
**Priority:** `P1` acts on something time-sensitive · `P2` substantive · `P3` depth

Last updated: **2026-08-06**

---

## Time-sensitive

| # | Item | Prio | Status | Note |
|---|------|------|--------|------|
| T1 | ILR consultation CP/N26/2 | P1 | OPEN | The draft abrogates eleven NIS1-era ILR regulations; CyberScale implements nine. Impact analysis and an independent observations section: [`docs/lu-n26-consultation-impact.md`](docs/lu-n26-consultation-impact.md). Consultation window closes — check the ILR notice for the date. |
| T2 | PR 530 merge unblocks Phase 0 step 3d | P2 | BLOCKED | [vulnerability-lookup#530](https://github.com/vulnerability-lookup/vulnerability-lookup/pull/530) is OPEN at `c5ccb64`. Until it lands there is no `POST /api/exploit-hazard` to use as an optional single-CVE backend. |

---

## Decision pending

| # | Item | Prio | Status | Note |
|---|------|------|--------|------|
| **P1** | **Phase 2 contextual severity — keep, rebuild, or retire** | P1 | **DECISION REQUIRED** | Synthesis of D3/D10/D11/D12/D13/D14 in [`docs/phase2-assessment-2026-08-06.md`](docs/phase2-assessment-2026-08-06.md). One line: **Phase 2 scores worse than ignoring context** — CVSS passthrough 36.95 % against the model's 34.2–34.6 % on the external set. Four options costed (ship de-escalation at ~49 % · rebuild the corpus so it can de-escalate · retire the model for CVSS + scope rules · stop and reconsider). Recommendation is **retire, with a deliberate pause before rebuilding**, because the de-escalation semantics must be authored as a regulatory question rather than inferred from 378 examples — inferring them is how the current corpus came to be wrong. Nothing acted on. |

## Data integrity

| # | Item | Prio | Status | Note |
|---|------|------|--------|------|
| D1 | 27 invalid `entity_type` values in curated datasets | P2 | **DONE** | Root cause was wider than the datasets: **11 of 22** entity types in `lu_thresholds.json` used ILR-flavoured names absent from the canonical vocabulary, so those LU thresholds were **unreachable in production** — the MCP boundary rejects them, while benchmarks pass by calling the assessor directly. Vocabulary aligned, 23 multi-entity sector-names mapped to real types, 4 LU scenarios corrected, 10 regression tests added |
| D6 | `evaluation/benchmark_curated.py` is not reproducible | P2 | **DONE** 2026-08-05 | *Found closing D1.* Three consecutive runs on identical data gave T 91.3 / 93.5 / 91.3 % and matrix 89.1 / 91.3 / 89.1 % — the benchmark loaded the **deprecated** ML T-model from `data/models/technical`, which retains MC dropout, rather than the `derive_t_level()` that replaced it in v4 and that `tools/incident.py` actually calls. Rewired to the deterministic path; three runs now give an identical **T 93.5 %, O 80.4 %, matrix 84.8 %** over 46 incidents. The deprecated models stay reachable behind `--compare-ml`, for comparison only. Pinned by `src/tests/test_curated_benchmark_determinism.py`, which also asserts `main()` does not drift back to the ML arm. |
| D7 | ILR/N21/1 is unreachable by construction | P3 | OPEN | *Found closing D1.* Every canonical entity type in `digital_providers` is an IR (EU) 2024/2690 entity type, and `is_lu_covered()` returns False for IR entities because EU thresholds take precedence. The LU mapping can never fire and `_assess_digital_service_providers()` is dead code. Documented in `lu_thresholds.json` under `unreachable_mappings` rather than removed, since it records the regulation's scope — which the draft N26 abrogates anyway. |
| D2 | Decide the fate of `contextual_train.csv` | P3 | **DONE** 2026-08-05 | **Deleted — but the premise was wrong.** The entry said "delete if the HuggingFace v4 dataset is sufficient provenance". There is no v4 on HuggingFace: `eromang/cyberscale-contextual-training` serves **v2** under the name `contextual_training.csv` (sha256 `d3ff30f8…`, byte-identical to the local `contextual_training_v2.csv`, uploaded 2026-03-31, before v3 and v4 existed). What actually justifies deletion is stronger: v4 is **byte-reproducible** from published + tracked inputs — regenerated and hash-matched to `090bd96c…` from `training_cves.csv` (published byte-for-byte on HF), tracked `sector_severity_rules.json` and tracked `contextual_cls.json`, generation being seeded. v3 is provenance for nothing shipped — the deployed model is v4-trained (see D8: the "reverted retrain" reading was my error). It stays in git history at `106ad93`; removal frees working-tree bytes only, not clone size. |
| D8 | ~~Deployed contextual model has zero entity-vocabulary overlap with production~~ | — | **INVALID** 2026-08-06 | **My error, not a defect.** The claim rested on `git diff c51b3b6~1 HEAD -- data/models/contextual/` returning empty. **`data/models/` is gitignored** — that diff compared nothing, and I read the absence of evidence as evidence of identity. The two commits it named touched only stray HuggingFace `.cache/` metadata; no weights were ever committed, so nothing was retrained-then-reverted. The deployed model is **v4-trained** and has seen all 59 entity types. Three independent proofs: dates (v4 03:48 → weights 05:31 → metrics 06:59, 1 Apr); evaluating it on the v4 test split reproduces `metrics.json` to four decimals on five metrics (81.71 %, macro F1 0.8148); and it scores 80.67 % on the 1,500 rows whose `entity_type` is absent from v2 entirely. Harness kept as `evaluation/eval_contextual_vocabulary.py`. **The real finding moved to D3/I3.** |
| D9 | HuggingFace contextual dataset is mislabelled | P2 | OPEN | *Found closing D2.* `eromang/cyberscale-contextual-training` serves v2 under a name that reads as current, and its card declares `size_categories: 1K<n<10K` for a 32,000-record file while documenting a two-column schema the file does not have. Fixing means re-uploading to a public repo — an outward action, so it needs an explicit go-ahead. Note the repo `README.md` does not advertise this dataset, which limits the exposure. |
| D10 | **Errors concentrate on escalated rows, not on thin entity types** | P1 | **RESOLVED** 2026-08-06 | *Found working D3, 2026-08-06.* The deployed model scores **94.28 %** (2,852/3,025) on rows whose label equals the CVSS base severity and **60.28 %** (1,070/1,775) on rows where the rule chain moved it — a 34-point gap, and by escalation depth: +1 step **57.4 %**, +2 **73.2 %**, +3 **29.4 %**. Coverage explains nothing by comparison: Spearman rho between corpus rows per entity type and test accuracy is **+0.057** across all 59 types. The eight *least*-covered types (95–112 rows, fewer than the three D3 flags) score 73.7–100 %, while `space_operator` with 1,304 corpus rows scores 68.4 %. So the model has not learned the escalation chain (sector trigger + cross-border + CER + impact) to the depth the labels encode; it is close to a CVSS passthrough and degrades exactly where context is supposed to matter — which is the product's whole claim. This subsumes D3. **Resolved the same day by writing the rule** (`evaluation/derive_contextual_rule.py`, ~60 lines, reads only `input_text`): on the frozen split the rule scores **87.65 %** against the model's **81.71 %**. Decomposed: on the **3,178 rows (66.2 %) the input fully determines**, the rule is **100.00 %** and the model **92.64 %** — the model gets 234 rows wrong whose answer is computable from what it was shown, which is a learning failure and not data noise. On the remaining **1,622 rows (33.8 %)** the generator's cross-border escalation is a **coin flip never written into `input_text`**, so no predictor seeing that string can recover it; rule 63.44 %, model 60.30 %, both near the base rate (the flip fired 36.6 %, not 50 %, because class balancing drops escalated rows preferentially). Pinned by `src/tests/test_contextual_rule_beats_model.py`. Follow-on: D11. |
| D13 | **The contextual model is not reproducible at `mc_passes=1`** | P2 | OPEN | *Found 2026-08-06 installing the external benchmark.* Two consecutive runs over the same 820 scenarios: rule **279 both times**, model **284 then 280**. Setting `mc_passes=1` does not make `ContextualClassifier.predict()` deterministic, so any published contextual figure carries a few answers of run-to-run noise and small deltas are not safe to interpret. Same class as D6, which was resolved by removing the MC-dropout path from the curated benchmark; here the path is production code, not benchmark code. |
| D14 | **Phase 2 cannot receive the field the expert judges on** | P1 | **DONE (interface)** 2026-08-06 | *Found 2026-08-06 writing the de-escalation rules (`docs/de-escalation-rules.md`).* Two rules derived from the expert's own `threshold_matched` formulas, thresholds fixed on a 60 % split and reported on the untouched 40 % (329 scenarios). Held-out: CVSS passthrough **34.04 %**, **R1 alone 41.95 %**, R1+R2 **48.63 %**; on the 149 down-graded scenarios, R1 alone 24.83 % against R1+R2 **55.70 %**. **R1 alone already beats the deployed model** (34.2–34.6 %) using only the entity's regulatory scope, which the system already knows. R2 needs `deployment_context` — *home* 96.6 %, *personal* 91.1 %, *single* 89.2 %, *workstations* 71.2 % of scenarios containing the term are down-graded — and **`ContextualClassifier.predict()` has no such parameter**. It takes the CVE description, sector, member states, score, entity type, CER flag and impact fields. So the largest remaining deficit is an **interface** problem: the information the expert judges on never reaches Phase 2, and no retraining closes that. The change is a free-text `deployment_context` argument. ⚠️ None of this is deployable as an answer — 48.63 % on four classes against a 25 % chance level is a diagnosis, not a working system. **Interface shipped 2026-08-06:** `deployment_context` and `apply_de_escalation` added to `ContextualClassifier.predict()` and to the MCP tool `assess_contextual_severity`. Three decisions worth not relitigating. (1) The context is **never fed to the model** — it drives a deterministic step on the output, because the deployed weights trained on a corpus containing no deployment context and injecting one would be out-of-distribution input; pinned by `test_the_model_input_is_untouched_by_deployment_context`. (2) De-escalation is **opt-in, default off**: enabling it changes the answer for every out-of-scope entity, and nothing measured here exceeds 49 %. (3) A downgrade is **always reported** in `key_factors` — a regulatory tool may not lower a severity silently. Also fixed in passing: the MCP docstring already promised "deployment context" while no such parameter existed. 10 new tests, 577 pass; 6 contract assertions updated because the kwargs genuinely changed. **What remains open is the decision to turn it on**, which is D14's real content and not a coding task. |
| D12 | **Neither the model nor the rule transfers to expert judgement** | P1 | OPEN | *Found 2026-08-06 recovering the predecessor dataset.* The CVE-Severity-Context scenarios were **not lost** — they were in the vault at `01_Projects/CVE-Severity-Context/` and deleted on 2026-03-30 by `de139e5a2` ("remove closed predecessor project"). Restored from git history: 1,357 files, 842 scenarios, 820 usable (5 have no CVSS score and were excluded rather than defaulted; 17 have no mappable sector). Scored with **matched inputs** — the four fields `ContextualClassifier.predict()` exposes, given identically to both arms: | synthetic | human | rule **73.46 %** / model **65.30 %** | rule **34.02 %** / model **34.63 %** |. Against the only independent ground truth that exists, **both arms sit barely above the 25 % chance level — and both are beaten by doing nothing at all.** Reporting the raw CVSS band scores **36.95 %** on the same 820 scenarios, against the model's 34.2–34.6 % and the rule's 34.02 %. The contextual apparatus subtracts two to three points from the input it is meant to improve. The structural reason is visible in the data before either arm runs: the expert moves severity **downward in 378 of 842 scenarios (44.9 %)**, upward in only 157, and **CyberScale's rule chain can only escalate** — sector, cross-border, CER and impact all push up, nothing pushes down. The most common expert judgement is therefore inexpressible by the rule and absent from every row the model trained on. ⚠️ **Unexplained contradiction, do not resolve by assumption:** `evaluation/predecessor_benchmark.md` (2026-03-28) reports contextual-**v1** at **88.0 %** on 1,833 of these same scenarios using the same four fields. v4 scores 34.63 % today. The invocation is controlled for (the production interface reproduces 65.30 % on synthetic, so it works, it is merely given less than the direct tensor path). **RESOLVED 2026-08-06 — the 88 % is not reproducible and the report is unreliable.** The circularity hypothesis was tested and **refuted**: only 17-18 of the 140 predecessor CVEs appear in either training corpus (incidental overlap) and **zero** of its `deployment_context` strings occur in any `input_text`. v1 did not train on them. What settles it instead is a subset comparison. Seven scenario types have **identical counts** in the recovered set and in the March report — banking 42, energy 45, health 61, manufacturing 33, public-admin 60, transport 29, water 8 — and they are exactly the types March reported at **100 %** (97 % for manufacturing). On those same 278 scenarios today: **model 32.0 %, rule 32.0 %**, ranging 12–48 % per type. A four-class model does not score 100 % on six independent sector sets; that figure is a defect signature, and this repo has the precedent — D6 found a benchmark silently loading the wrong model, D4 found header figures that were noise. The mechanism cannot be pinned because v1's weights no longer exist locally (`~/Documents/GitHub/CyberScale/data/models/contextual` holds a **copy of v4**, same metrics to four decimals). **`evaluation/predecessor_benchmark.md` should be treated as void, and its 88.0 % must not be quoted.** Note the five remaining types were 2-3x larger in March (cross-border 340 vs 140, enterprise 339 vs 128, digital-infrastructure 339 vs 128, small-deployment 308 vs 128, essential-service 229 vs 40), so the vault copy is a pruned version — but that cannot explain 100 % on the seven identical ones. |
| D11 | **Retire the contextual ML model in favour of the rule** | P1 | OPEN | *From D10, 2026-08-06.* Lesson 18's own test now applies to Phase 2 exactly as it did to the T- and O-models, both since removed: the label is deterministic from the inputs, and the rule outscores the model by **5.94 points** while being perfect wherever the answer exists. Removing it drops a 1.7 GB artifact, its training run, its GPU dependency and its 20-epoch retrain cycle, and makes the output explainable — which matters for a regulatory qualification tool in a way accuracy alone does not. **Two things to settle before acting.** (a) The rule was validated against the *synthetic* corpus, whose labels it reimplements; that is circular by construction, so the comparison that decides deployment is against the **curated incident benchmark**, not against generated data. **UNBLOCKED 2026-08-06 — dataset recovered from git history, and the answer is not the one expected: see D12.** Superseded text follows. **BLOCKED on missing data, established 2026-08-06.** The only independent ground truth for contextual severity is the **CVE-Severity-Context** predecessor dataset — 1,890 human-curated scenarios, authored by a different project, therefore not circular. It is **not on disk**: searched `~/Documents` and `~/Downloads`, absent; `evaluation/benchmark_predecessor.py` still points at `../CVE-Severity-Context/Dataset/Scenarios`. What survives is a report dated 2026-03-28 showing **contextual-v1 at 88.0 %** on 1,833 of those scenarios against the predecessor's own 80.7 % baseline. Two consequences, both uncomfortable. First, that report predates v4 by three days, so **the deployed model has never been scored against non-synthetic ground truth** — its 81.71 % is entirely self-referential. Second, 88.0 % on human-curated data is *higher* than 81.71 % on synthetic, which is weak evidence that the model generalises better than the synthetic benchmark suggests, and is a reason to hold the retirement rather than press it. Recovering the dataset (it is presumably a repo of the author's) is the single blocking action. (b) The 33.8 % coin-flip share is an artifact of `cross_border_escalation_prob: 0.5` in the generator, not of the regulation — worth deciding whether the corpus should record the draw, or the rule should apply cross-border escalation deterministically. |
| D3 | Three IR re-routed entity types underperform — **coverage is not the cause, retrain refuted it** | P1 | **CLOSED (by re-diagnosis)** 2026-08-06 | *Was "thin training coverage"; upgraded to P1 on measurement 2026-08-06.* `ixp_operator`, `public_ecn_provider` and `public_ecs_provider` score **66.0 % (33/50)** on the v4 test split against **81.7 %** overall. Wilson 95 % intervals [52.2, 77.6] vs [80.6, 82.8] **do not overlap** — the gap is established despite the small n. Per type: 63.2 % (n=19), 60.0 % (n=15), 75.0 % (n=16). The doc's earlier reading — thin coverage, "not a blocker" — is superseded: this is not a coverage worry, it is a measured deficit on exactly the types the IR scope fix made the model answerable for. Fix is a retrain with those three topped up; `evaluation/eval_contextual_vocabulary.py --by-entity-type` is the before/after gate. **2026-08-06 — the coverage premise does not survive measurement (see D10).** The deficit is a composition effect: focus rows escalate away from the CVSS base severity in **54 %** of cases against **37 %** for the whole test set, and the model scores 60.3 % on escalated rows generally. Holding escalation constant, the residual focus gap is −8.6 pts on escalated rows (14/27) and −11.8 on non-escalated (19/23) — n too small to conclude. The top-up retrain was run as the empirical test, on a frozen eval set. **It refuted the coverage hypothesis outright.** Tripling the three types (137/149/132 -> 472/428/406 rows) produced, on the identical frozen v4 test split: overall **81.71 % -> 80.79 %** (-44 correct answers), macro F1 0.8148 -> 0.8061, **29 of 59 types degraded against 17 improved**. On the targeted types themselves, **two of three did not move by a single answer** — `ixp_operator` 12/19 -> 12/19 and `public_ecn_provider` 9/15 -> 9/15, byte-identical outcomes after 3x the data. Only `public_ecs_provider` improved, by 2 answers out of 16. Net: **+2 on the target, -46 on the other 56 types.** Caveat stated rather than buried: v5 trained on ~4,122 fewer rows because the frozen eval set was excluded, which handicaps it and explains part of the -46 — but it does not explain the two targeted types producing identical results, which is the decisive observation. **The deployed model was NOT replaced** (candidate in `data/models/contextual-v5-candidate/`, deployed model backed up and hash-verified at `contextual-v4-backup-20260806/`). The remaining item is D10. |
| D4 | `evaluation/curated_benchmark.md` header figures are stale | P3 | **DONE** 2026-08-05 | Resolved with D6, and the figures recorded here were themselves noise. The 97.5 / 96.2 % dated from 2026-03-30, before v4/v5/v8; the 95.7 / 82.6 / 91.3 % that replaced them came from one MC-dropout run. Regenerated deterministically: **T 93.5 %, O 80.4 %, matrix 84.8 %**. The six additions INC-041–046 do score 6/6 on both axes; the claim that they *raised the averages* was withdrawn — that comparison was against a nondeterministic baseline. Four pre-existing 2024 incidents (INC-010/029/033/039) carry 4 of the errors. Every T and O error is off by exactly one level. |
| D5 | `data/training/` vs `training/data/` is a confusing pair | P3 | OPEN | One is tracked and holds a single superseded file; the other is gitignored and holds the working corpus. A byte-identical duplicate survived in the gap until 2026-08-05. Consider renaming one. |

---

## Regulatory — Luxembourg

| # | Item | Prio | Status | Note |
|---|------|------|--------|------|
| R1 | Switch LU to N26 on entry into force | P1 | BLOCKED | `lu_n26.py` is implemented and tested (14 tests) but **not wired into the router**. `lu.py` remains the default because the draft is not in force. Switching should be a deliberate act on the adoption date, not a side effect. |
| R2 | No implemented rule for electronic communications providers | P2 | OPEN | With IR correctly scoped, LU telecom falls to the general ML model. The applicable national text is **ILR/N23/3**, which CyberScale never implemented and which N26 also abrogates. Under N26 it would fall to the generic Art. 2(1) criteria. |
| R3 | Four N26 criteria need fields the impact taxonomy lacks | P2 | OPEN | Trade-secret exfiltration (d), physical access compromise (f), direct financial loss and turnover in euro (j), prejudice amounts (k). `lu_n26.py` accepts them as optional and reports them in `not_evaluable`. Adding them to `impact_taxonomy.json` is the real fix. |
| R4 | N26 Art. 2(4) recurrence needs incident history | P3 | OPEN | Two occurrences in six months sharing a root cause, collectively meeting criterion (j). CyberScale assesses one incident at a time and holds no such state. `assess_recurrence()` is exposed separately so the requirement stays visible rather than silently unmet. |
| R5 | N26 annexes are unpublished | P3 | BLOCKED | Art. 2(1)(l) and 2(5) let ILR reintroduce sector-, entity-type- or group-specific criteria by annex. None shipped with the consultation draft, so criterion (l) is permanently `not_evaluable`. This is the route by which the nine abrogated sector regulations could effectively return. |

---

## Exploit hazard integration

Design and evidence: [`docs/exploit-hazard-integration-design.md`](docs/exploit-hazard-integration-design.md).
Underlying validation: sibling project `../Exploit-Hazard-Validation/`.

| # | Item | Prio | Status | Note |
|---|------|------|--------|------|
| E1 | Step 1 — fix EUVD exploited set | P1 | **DONE** | `get_exploited()` paginates `/search?exploited=1` (1,665) instead of the 4-record "latest" view |
| E2 | Step 2 — CIRCL KEV as exploitation source | P1 | **DONE** | EUVD's exploited flag is 99.7% CISA. CIRCL KEV holds 2,789 distinct CVEs with per-source provenance |
| E3 | Step 3a-3c — Phase 0 prioritisation | P2 | **DONE** | Ranking only. Benchmark reproduces the validation's AUC-ROC in 11/11 cells exactly |
| E4 | Step 3d — CIRCL endpoint as optional backend | P3 | BLOCKED | See T2. Right shape for single-CVE explain-this-one calls, wrong for ranking an inventory |
| E5 | Step 4 — dependency-graph correlation | P3 | OPEN | The paper defers correlated exposure to future work (§5.3, "a graph-based dependency structure"). `aggregation.py:53 propagate_cascading()` already walks the ENISA/CER sector graph for incident impact. Research track, no delivery date. |
| E6 | Never aggregate hazard with the independence product | P2 | OPEN (guard) | Not a task, a constraint. Measured overstatement reaches **247×** per component (Linux kernel: `EL_g` 0.79 when the most exploitable CVE scored 0.0032). Fleet-wide it is mild (1.14–1.30×). If a per-entity or per-sector hazard rollup is ever requested, this is the trap. |

---

## Testing and infrastructure

| # | Item | Prio | Status | Note |
|---|------|------|--------|------|
| I1 | `be.py` evaluated only `thresholds[0]` | P2 | **DONE** | All three availability thresholds now evaluate, plus the maintenance exclusion. Surfaced two wiring gaps: the router never passed `affected_persons_pct`, and `0.0` was indistinguishable from "unmeasured" |
| I2 | Full suite needs `poetry install` | P3 | **DONE** | 545 pass. `src/tests/store` needs chromadb and the model tests need torch; a light venv skips them cleanly rather than failing |
| I3 | Validate ML inference for the three re-routed entity types | P2 | **DONE** 2026-08-06 | Validated, and it failed: 66.0 % vs 81.7 %, intervals disjoint. Result carried into D3. |

---

## Open questions

| # | Question | Status | Note |
|---|----------|--------|------|
| Q1 | Are the 23 multi-entity `entity_type` values a mistake or a different convention? | **RESOLVED** | A mistake. In all 23 cases `entity_type == sector` exactly — the generator filled the field with the sector name where it had no specific type. Six of the nine sectors have exactly one valid type, and the remaining three were resolved from the incident descriptions. |
| Q2 | When does N26 enter force, and does it change on consultation feedback? | OPEN | Gates R1. The criteria implemented in `lu_n26.py` are the *draft*; adoption may alter them. |
| Q3 | Will the annexes reintroduce sector-specific thresholds? | OPEN | Gates R5 and determines whether the nine abrogated regulations effectively survive in another form. |
| Q4 | Does the ML model perform adequately on the three re-routed entity types? | **RESOLVED (no)** | **No.** 66.0 % vs 81.7 %, Wilson intervals disjoint (2026-08-06). The IR scope fix did trade a wrong deterministic answer for a measurably weaker probabilistic one. That is still arguably the right trade — a wrong answer delivered confidently is worse — but it is now a measured cost, not an assumption. Drives D3. |
| Q5 | Should Phase 0 default `k` track the validation as catalogues age? | OPEN | `k` drifts with observation date (0.684 Jan 2025 → 0.576 Aug 2026). The 0.55 default is a 2026-08 ranking choice, not a constant. Re-check when the validation re-runs. |

---

## Decisions taken

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-08-05 | Exploitation status comes from CIRCL KEV, not EUVD | EUVD's exploited set is 99.7% CISA with an identical median TTE. Reaching for EUVD looks like a sovereignty decision and does not function as one. |
| 2026-08-05 | IR scope follows Article 1, not our entity list | `ir_incident_thresholds.json` listed 14 entity types; IR (EU) 2024/2690 Art. 1 names 11 and Arts. 5–14 cover exactly those. Verified against the Official Journal text. |
| 2026-08-05 | `exploited` is three-valued | True and False are catalogue answers; None means the lookup failed. Collapsing the last into False reports "not exploited" on every network error. |
| 2026-08-05 | `time_to_exploit_days` stays signed | Exploitation before publication is a real category, not something to clamp away. |
| 2026-08-05 | Age is a function, not a record field | `age_days(record, as_of=)` — a value that changes between calls does not belong in something callers may cache. |
| 2026-08-05 | `affected_persons_pct` is `float \| None` | `None` means the entity could not determine the count, which is what BE's `unknown_scope` threshold exists for. `0.0` asserts no users were affected. The old `0.0` default collapsed the two. |
| 2026-08-05 | `lu_n26.py` sits alongside `lu.py` rather than replacing it | The draft is not in force. Replacing in-force rules with a consultation draft would be wrong; a parallel module makes adoption a configuration change. |
| 2026-08-05 | Phase 0 emits no rate, count or probability | The model's rate output is denominated in third-party sensor detections and runs ~55× above any observable feed. Not reportable. |
| 2026-08-05 | Phase 0 `k` defaults to 0.55, not the paper's 0.605 | 0.605 falls outside every properly-estimated interval (0.52 curated / 0.72 observational), and for ranking lower k performs better. Stated as a ranking choice, not a calibration claim. |
| 2026-08-05 | Unrankable and unresolvable entries are excluded, not sorted last | Placing an unrankable vulnerability at the bottom of a remediation queue asserts it is least urgent. |
| 2026-08-05 | Real incidents go to `real_incident_validation.json`; `curated_*` stay synthetic | Except `curated_incidents.json`, which was always real-world. Follows the project's own README. |
| 2026-08-05 | `entity_type` vocabulary is `nis2_entity_types.json`, full stop | It is what the MCP boundary validates against, so anything else describes behaviour production cannot reach. `lu_thresholds.json` now maps *from* canonical names; ILR's own vocabulary lives in the regulation, not in a routing key. |
| 2026-08-05 | Dead mappings are documented, not deleted | `lu_thresholds.json` keeps the `digital_providers` entries under `unreachable_mappings` with the reason. Deleting them would lose the record of what ILR/N21/1 covered; leaving them undocumented would mislead. |
| 2026-08-05 | BE-12 kept as a negative case rather than made to pass | The AZ Monica reporting gives no duration, and every BE availability threshold needs one. Supplying a plausible figure would be inventing evidence. Assessing real incidents from open reporting routinely lacks the fields criteria are written around. |

---

## Dropped

| Item | Reason |
|------|--------|
| Consume CIRCL's `POST /api/exploit-hazard` for Phase 0 ranking | Right for single-CVE third-party lookups, wrong for ranking an inventory — one HTTP call per CVE, when the age term is four locally-validated lines. Retained as E4, an optional backend for single-CVE calls. |
| Adding EPSS as a Phase 1 feature | Would blur severity and likelihood together. Wrong on the modelling side, and wrong under NIS2 where Art. 23 significance is impact-based. Became the separate Phase 0 surface instead. |
| Proximus/Tango as an LU curated scenario | Its RETEX records `nis2_significant: false`, but under the IR criteria a 4-hour degradation would trigger. Resolving that needs an EECC-versus-NIS2 determination the vault does not make. |
